convertLPJml_rev21 <- function(x, subtype) { # nolint: object_name_linter.
  stop("Convert functions for LPJml are not written yet due to missing aggregation weights. Set convert=FALSE")
  return(x)
}
